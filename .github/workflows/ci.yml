name: Python CI

on: [push]

jobs:
           python-version: 3.11  # Use a stable Python version
          
      - name: Install dependencies
 build:
    runs-on: ubuntu-latest  # Change to 'windows        run: |
          python -m pip install --upgrade-latest' if pywin32 is mandatory

    steps:
 pip
          # Exclude pywin32 for non-Windows systems      - uses: actions/checkout@v2
      - name: Set up
          if [ "$(uname)" != "Windows_NT" ]; then
            sed -i Python
        uses: actions/setup-python@v2
        with '/pywin32/d' requirements.txt:
          python-version: 3.10  # Use a valid Python version
      - name
          fi
          pip install -r requirements.txt: Install dependencies

          
      - name: Check Python version
        run: |
          python -m pip install --upgrade        run: python --version
